<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.threeao4.greenpentadudes.beerd.MainActivity"
    android:background="@mipmap/beer_bg"
    android:onClick="onButtonClick">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@mipmap/beer_bg">

        <ImageView
            android:layout_width="fill_parent"
            android:layout_height="200dp"
            android:id="@+id/imageView"
            android:src="@mipmap/beer_logo"
            android:layout_weight="1" />

        <ImageButton
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:id="@+id/searchpageButton"
            android:layout_gravity="center_horizontal"
            android:src="@mipmap/search_button"
            android:layout_weight="1"
            android:background="#00000000"
            android:onClick="onsearchButtonClick" />

        <ImageButton
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:id="@+id/prevsearchButton"
            android:background="#00000000"
            android:src="@mipmap/prevsearch_button"
            android:layout_weight="1"
            android:nestedScrollingEnabled="false"
            android:onClick="onprevsearchButtonClick" />

        <ImageButton
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:id="@+id/geninfoButton"
            android:background="#00000000"
            android:src="@mipmap/geninfo_button"
            android:layout_weight="1"
            android:nestedScrollingEnabled="true"
            android:onClick="ongeninfoButtonClick" />

    </LinearLayout>
</RelativeLayout>